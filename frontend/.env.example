# Copy to .env.local to point the dashboard at a running backend.
#
# Unset or "true" keeps the app on its built-in deterministic mock, which is what makes the
# demo work with nothing else running. Set to "false" to use the real forecasting API.
VITE_USE_MOCK=false
VITE_API_BASE_URL=http://localhost:8000
