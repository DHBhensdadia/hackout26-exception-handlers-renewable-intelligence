# Base path for static hosting under a sub-directory (default: "/")
VITE_BASE_PATH=/

# FastAPI backend origin used when mock data is disabled
VITE_API_BASE_URL=http://localhost:8000

# "false" => call the real backend. Anything else (or unset) => use mock data.
VITE_USE_MOCK=true
